# Workspace Index

This index is machine-generated to help future agents answer high-level
questions about this workspace. It is a *directive scaffolding* — it points
at which repos are relevant; it is NOT a substitute for reading the repos.

**How to use this index:**
1. Read the per-repo blocks below. They are the source of truth.
2. The concept map at the bottom is a convenience index, not a filter.
   Repos often touch concepts not listed there. Never conclude a topic
   isn't covered without scanning the per-repo blocks.
3. Each block names domains, entry points, and "start here" paths. Use
   those as the starting point for a repo-specific dive.
4. The `Description` field in each block carries business and product
   context that doesn't fit in structured fields — read it.
5. Treat the "Recent substantial changes" line as a hint, not a changelog.

_Last regenerated: <TIMESTAMP>_

---
